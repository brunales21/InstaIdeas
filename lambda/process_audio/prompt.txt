The user has submitted the following idea explanation:

"{texto_transcrito}"

Your task is to transform this explanation into a clean, structured JSON using the schema below.
The output language must ALWAYS match the language of the transcription.
Return ONLY valid JSON.
Do not add any text outside the JSON.
Do not include explanations, comments, or meta text.

JSON SCHEMA (use exactly these fields):

{
  "title": "",
  "possible_names": [],
  "description": "",
  "problem": "",
  "solution": "",
  "suggested_improvement": "",
  "potential_impact": "",
  "target_audience": [],
  "usage_scenarios": [],
  "extra_opportunity": "",
  "extra_context": ""
}

---------------------------------------------
RULES FOR EACH FIELD
---------------------------------------------

1. title
   - If the user did not provide a direct title, generate a clear, concise one.
   - Base it strictly on what the user described.
   - Do not invent new functionalities.

2. possible_names
   - Provide exactly 3 original name ideas for this concept.
   - Names must reflect the core of the idea.
   - Do not invent features not mentioned by the user.

3. description
   - Write a cohesive, understandable summary of the idea.
   - Use only what the user expressed explicitly or implicitly.
   - Do NOT change the direction of the idea.
   - If information is missing, keep the description concise.

4. problem
   - Extract the real problem described or implied.
   - If the user did not explicitly describe a problem, infer a reasonable one based on the idea’s purpose.
   - Do not fabricate unrelated problems.

5. solution
   - Use the user's solution if they described one.
   - If the solution was NOT given, generate a logical solution aligned with the idea.
   - If the solution is generated by you, append this exact string at the end: " (generated suggestion)".

6. suggested_improvement
   - Suggest ONE improvement that enhances the idea without altering its essence.
   - The improvement must feel natural and aligned with what the user described.
   - Do not introduce features that contradict or expand the core concept excessively.

7. potential_impact
   - Provide a short motivational statement describing the positive impact the idea could have.
   - Focus on usefulness and benefits.
   - Keep it inspiring but realistic.

8. target_audience
   - List 2–4 groups of people who would naturally benefit from this idea.
   - Do not invent unrealistic or unrelated audiences.

9. usage_scenarios
   - List 2–3 realistic situations where this idea would be especially useful.
   - Keep them practical and aligned with the idea.

10. extra_opportunity
   - Provide one additional opportunity or angle that fits naturally with the idea.
   - This should NOT introduce unrelated features.
   - Keep it light and helpful.

11. extra_context
   - Add any extra details the user mentioned that do not fit into other fields.
   - Leave empty ("") if there is nothing additional.

---------------------------------------------
IMPORTANT
---------------------------------------------

- Output must be ONLY the JSON.
- No markdown.
- No quotes outside the JSON.
- No explanations.
- No commentary.
- No “here is your JSON” statements.
- Language of the output MUST match the language of the transcription.

